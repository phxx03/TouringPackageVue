import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'TourPackageVue',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
